# agent-sla-watch

SLA/SLO monitor for agent latency and success rates.

## Install

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Usage

```bash
agent-sla-watch --task "demo"
```
